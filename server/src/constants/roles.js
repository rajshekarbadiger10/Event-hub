/** Keep in sync with shared/src/constants/roles.ts */
export const USER_ROLES = {
  CUSTOMER: 'customer',
  VENDOR: 'vendor',
  ADMIN: 'admin',
}

export const USER_ROLE_LIST = Object.values(USER_ROLES)
